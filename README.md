# Spark-Scala-BteqConversions

Run the below command , after the jar is built using maven.

spark-submit --class "com.bteq.BteqConvert" --master "local[*]" pathtojar/bteq-spark-0.0.1-SNAPSHOT-jar-with-dependencies.jar
